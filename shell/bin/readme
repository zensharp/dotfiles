#!/usr/bin/env bash

function upsearch () {
    test / == "$PWD" && return || test -e "$1" && echo "$PWD" && return || cd .. && upsearch "$1"
}

RESULT=$(upsearch "README.md")

if [ -z "$RESULT" ]
then
	echo "README not found"
else
	READER=${READER:-cat}
	$READER $RESULT/README.md
fi
