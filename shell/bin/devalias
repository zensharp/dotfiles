#!/usr/bin/env bash

set -e
shopt -s globstar

name=$1
target=$2

if [ -z "$name" ] || [ -z "$target" ]
then
	echo "Usage: devalias <name> <target>"
	exit -1
fi

path=$(find **/bin/**/"$target")
path=$(realpath $path)

command="alias $name=$path"
echo -e $command
